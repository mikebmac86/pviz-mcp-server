# pviz MCP Server Dependencies

# MCP SDK (SSE transport support)
mcp>=1.6.0

# HTTP client for API calls
httpx>=0.27.0

# ASGI stack (MCP SSE examples use newer Starlette/Uvicorn)
starlette>=0.46.1
uvicorn>=0.34.0

# Optional: nicer JSON logs
python-json-logger>=2.0.7
