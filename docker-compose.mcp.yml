services:
  pviz-mcp-server:
    build:
      context: .
      dockerfile: mcp_server/Dockerfile
    restart: unless-stopped
    environment:
      PVIZ_API_URL: http://api:8000
      PORT: 8080
      HOST: 0.0.0.0
      LOG_LEVEL: info
    ports:
      - "8080:8080"
